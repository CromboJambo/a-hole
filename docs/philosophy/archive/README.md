# Archive

This directory holds legacy Crabjar code that is intentionally excluded from the supported build.

- `legacy/`: old experiments, parallel implementations, support scripts, and prototypes kept only for reference

Nothing under `archive/` is part of the current product contract.
